# Environment Configuration for Production Docker Deployment

# Application Environment
NODE_ENV=production
PORT=3001

# Application URL (change to your domain)
CLIENT_URL=http://localhost:3001

# Session Configuration (CHANGE IN PRODUCTION)
SESSION_SECRET=drawdb-secret-key-change-in-production

# Database Connection Limits
DB_CONNECTION_TIMEOUT=30000
DB_MAX_CONNECTIONS=50

# Docker Configuration
COMPOSE_PROJECT_NAME=drawdb

# VITE_BACKEND
VITE_BACKEND_URL=http://backend.com